import 'package:flutter/material.dart';

class HomeForm extends StatelessWidget {
  List catNames = ["Dental", "Heart", "Eye", "Brain"];
  List<Icon> catIcons = [];

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
